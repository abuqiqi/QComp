# qcomp 开发规范

本文档定义项目级代码与文档修改规范。
检查、规划或修改项目之前，必须完整阅读本文档。

## 开始工作前

1. 完整阅读本文档和 `README.md` 中与任务相关的章节。
2. 检查与任务相关的现有实现、测试和配置。
3. 将修改限制在用户要求的范围内，并保留无关工作。
4. 优先选择能够完成当前需求的最小设计。

## 代码规范

1. 每个源文件开头必须使用中文 docstring 或注释。Python 模块 docstring 先用一段话说明文件职责、输入输出数据流和与相邻模块的边界，再使用 `主要内容：` 标题和项目符号逐项列出主要类、函数、算法或导出接口；对象名称使用双反引号标记，后接中文说明。
2. 每个函数和方法必须使用中文 docstring 说明用途和参数；有实际意义时还要说明返回值和异常。
3. 多段 docstring 统一使用 `参数：`、`返回：` 和 `异常：` 标题。
4. 注释用于解释设计意图、数学含义、张量布局或不明显的约束，不逐行复述代码。
5. 公开接口必须添加类型标注；内部函数在有助于理解时也应添加类型标注。
6. 优先使用清晰的领域名称，不滥用缩写；函数职责应集中，控制流应直接。
7. 每项行为只保留一个权威实现；只有出现真实重复时才提取共享代码。
8. 不为当前不存在的需求增加推测性抽象、兼容层、fallback、占位代码或配置项。
9. 校验外部输入、持久化 artifact，以及可能静默破坏数值结果的假设；内部类型接口已经保证的条件不重复校验。
10. 保持依赖方向清晰：workflows 负责编排，representations 定义数据结构和数学操作，nn 定义可执行 PyTorch 模块的公共行为，backends 适配具体计算库，evaluation 负责测量，storage 负责持久化。
11. 只有依赖能够明显简化实现或提供必需能力时才引入；可选后端依赖必须与核心包隔离。
12. 编写或修改 `scripts/` 下的脚本前，先检查 qcomp 库已有的公共接口，优先复用现成的配置、workflow、数据加载、评测和日志功能，不在脚本中重新实现。脚本主要保留命令行参数与实验专属策略；已有上层入口能够完成任务时优先调用。
13. `scripts/` 下可直接执行的脚本必须在文件开头写明如何从项目根目录调用，至少给出一条可执行命令；存在常用参数、配置文件或不同运行模式时，同时给出代表性示例和必要说明。仅供脚本加载的模块及 CSS、HTML、JavaScript 等页面资源不要求调用说明。

## 文档规范

1. 使用正向流程描述当前设计、支持能力和预期用法。
2. 修改后删除过时说明和已被替代的内容，不保留旧实现的问题叙述。
3. 除非用户明确要求历史背景，否则不记录迁移过程、被否决方案或旧方案错误。
4. 示例必须可以执行，并与当前公开接口保持一致。
5. 优先提供精简说明、一个规范示例和指向权威章节的链接，避免重复描述。

## 测试与验证

1. 涉及数学逻辑、artifact 兼容性或后端行为时，必须增加或更新对应测试。
2. 张量测试使用小型确定性数据；必须使用随机数据时固定随机种子。
3. 先运行范围最小的相关检查，再在可行时运行完整项目检查。
4. 不通过削弱或删除无关测试来让修改通过。

## 完成工作前

1. 重新阅读修改后的文件，删除无用代码、过时文字、不必要校验和重复逻辑。
2. 确认全部注释和 docstring 使用中文，并准确描述最终行为。
3. 确认文档只呈现当前正向流程。
4. 运行项目配置的相关格式化、lint、类型检查和测试。
5. 汇报最终修改和验证结果，不叙述已被替代的实现。
