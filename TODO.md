0、之前看了一个Multiverse的工作，里面做了一些张量网络压缩的后续实验，我也放进你的后续工作了哈，可以跟CompactifAI的retrain作对比
Quantum-enhanced Large Language Models on Quantum Hardware via Cayley Unitary Adapters

1、然后后面你可以思考一下做一个自动化压缩的脚本，大概就是我拿到一个新模型，先做敏感度分析，然后自动化确定最优的压缩目标矩阵进行压缩。自动化的方式最简单就是根据敏感度排序，但是可能需要实验确定是少选几个尽可能往深了压缩，还是多压几个每个少压一些。然后可能还有一些更优的压缩方式，这个要看后面实验的结果


2、我看到你的文档里加了几个压缩方案的结果，效果不好的原因是不是压缩模块太多了？比如gsm这种很难找到96个影响比较小的模块吧，然后可能还有一些误差累加的问题；误差累加这个点处理比较简单的方式可能要在后续加了比较快的验证模块之后做串行的压缩损失测试，然后逐步做压缩？


3、感觉目前做敏感度分析的时候可以加一些指标，我感觉比较合适的一个是kl散度，这个在llm后训练的时候用的比较多，就是可以把原模型视为reference model，然后计算做了张量化奇异值截断后模型和reference model的kl散度，这个可以体现新模型的偏离程度。然后我觉得这个指标一个是可以在敏感度分析的时候提高一些效率，也可以用在后面retrain的时候作为一部分loss。这个点珑珊可以先尝试着加一下？然后可能还有一些更简单的方式比如这个里面提到的局部模块的nmse；我觉得可以做两个尝试，一个是高效的，就是类似这个文档里的分阶段筛选提高效率；一个是做加权，看看筛选的效果会不会变好


# 张量网络压缩的混合敏感度筛选方案

> 结合局部输出误差、原模型参考 KL 与 CompactifAI 的任务准确率曲线，在有限评测预算内选择待压缩模块及其压缩强度。
>
> 本文是一份待实验验证的方法设计。文中的样本量、候选数量和筛选规则均为建议起点，不代表 CompactifAI 的原始实现或已经验证的最优设置。

## 1. 目标与方法来源

目标是在给定参数量或存储预算下，确定哪些模块适合张量网络压缩，以及各模块应使用多大的 MPO 键维度，同时减少反复运行完整任务评测的开销。

CompactifAI 用 MPO 键维度控制压缩强度，并展示不同层的 MMLU 准确率随最大键维度变化的曲线；随后对压缩模型进行 healing 微调。原文没有完整披露逐次替换与还原、每个扫描点是否微调、曲线如何自动转化为键维度配置等细节。[CompactifAI 原文及补充材料](https://arxiv.org/html/2401.14109v2)

本方案保留这种“压缩强度—任务表现”的验证思路，并在前面增加两个较便宜的筛选阶段：

| 阶段        | 评价信号                           | 主要作用                                   | 需要运行的网络范围                       |
| ----------- | ---------------------------------- | ------------------------------------------ | ---------------------------------------- |
| A：局部粗筛 | 模块输出归一化平方误差 NMSE        | 缩小每个模块的压缩候选范围                 | 原模型收集激活后，主要运行待测模块       |
| B：输出筛选 | 相对未压缩 reference 的最终输出 KL | 判断局部压缩对最终预测的影响               | 每个候选运行模型，或运行受影响的后半部分 |
| C：任务确认 | 任务准确率与压缩强度曲线           | 检查 KL 排名是否符合目标任务，确认关键候选 | 少数候选运行固定的任务开发集             |
| D：联合验证 | 组合模型的 KL、任务表现与资源占用  | 检查模块相互作用，确定最终配置             | 少数完整压缩模型                         |

阶段 A、B 的加入、候选分配规则以及下文的单模块替换流程，都是本方案的设计。节约时间的来源是减少候选和评测数据量、复用计算；KL 本身不会省去候选模型的前向计算。

## 2. 实验对象与公平比较

### 2.1 模块粒度

第一版以一个可替换的线性模块为基本单位，例如 attention 中的 `q_proj`、`k_proj`、`v_proj`、`o_proj`，以及 MLP 中的 `gate_proj`、`up_proj`、`down_proj`。具体模块列表以目标模型实际结构为准。

所有单模块实验从同一个未压缩模型出发，每次仅替换一个模块，测完还原。这样得到的是该压缩操作在原模型中的影响。组合压缩后的条件敏感度在阶段 D 另行测量。

不要根据模块名称或层深直接排除候选；是否保留原状由测量结果决定。

### 2.2 固定张量化方式，用参数保留比例对齐候选

对每个模块固定权重索引拆分、排列顺序、MPO 核数量、分解算法和计算精度，第一轮只改变键维度。否则结构变化和压缩强度变化会混在一起。

定义参数保留比例：

$$
\rho_\ell(r)=\frac{P_\ell(r)}{P_\ell^{\mathrm{original}}}.
$$

其中，$r$ 是一个实际可实现的压缩配置，包含完整键维度列表；$\rho$ 越小，压缩越强。未压缩配置记为 $r=\mathrm{dense}$、$\rho=1$。

若 MPO 有 $d$ 个核，第 $k$ 个核形状为 $(r_{k-1},m_k,n_k,r_k)$，且 $r_0=r_d=1$，则核参数量为：

$$
P_\ell(r)=\sum_{k=1}^{d}r_{k-1}m_kn_kr_k.
$$

若存在 bias 或额外可训练参数，统计时一并计入。相同最大键维度在不同矩阵上不代表相同压缩率，跨模块比较应记录实际 $\rho$ 和节省参数量。

建议先尝试 $\rho\approx\{0.8,0.6,0.4\}$，映射到可实现的键维度后去重；同时保留 dense 选项。不能实现的比例无需强行匹配，记录实际值即可。

## 3. 数据与 reference 准备

| 数据用途                                    | 要求                                                         |
| ------------------------------------------- | ------------------------------------------------------------ |
| 校准集 $\mathcal D_{\mathrm{cal}}$          | 用于激活统计、局部 NMSE 和 KL；覆盖目标语言、领域及上下文长度 |
| 任务开发集 $\mathcal D_{\mathrm{dev}}$      | 用于阶段 C 的选择与阶段 D 的调参；固定题目、提示模板及评分协议 |
| Healing 训练集 $\mathcal D_{\mathrm{heal}}$ | 仅在需要微调恢复时使用，与最终测试数据隔离                   |
| 最终测试集 $\mathcal D_{\mathrm{test}}$     | 配置和训练策略冻结后使用，不用于反复选择键维度               |

可从 32–64 段、每段 256–512 tokens 的校准文本开始，再用另一批文本检查排名稳定性。长上下文任务应单独纳入有代表性的长序列；以上数量仅用于起步，不保证充分覆盖。

Reference 使用同一未压缩 checkpoint，固定 tokenizer、词表、模板和精度。比较 MPO 压缩时，应保持其他精度设置一致，避免同时混入量化影响。

在评估模式下运行 reference，按需收集模块输入及最终输出 logits。单模块测试期间无需为每个候选重新加载整个模型；保留原模块，在同一个工作模型中替换并还原即可。

## 4. 阶段 A：用局部输出 NMSE 粗筛

对模块 $g_\ell$ 的 reference 输入 $X_\ell$，比较原模块与压缩模块输出：

$$
E_\ell(r)=
\frac{
\sum_{x\in\mathcal D_{\mathrm{cal}}}
\|g_\ell(X_\ell(x))-\widehat g_{\ell,r}(X_\ell(x))\|_F^2
}{
\sum_{x\in\mathcal D_{\mathrm{cal}}}
\|g_\ell(X_\ell(x))\|_F^2+\epsilon
}.
$$

求和只包括有效位置。对于 bias 保持不变的线性层，分子可简化为 $\sum_x\|(W_\ell-\widehat W_{\ell,r})X_\ell(x)\|_F^2$。这种指标考虑了实际输入；SVD-LLM 对输入激活与压缩重构误差关系的讨论提供了相关依据。[SVD-LLM](https://arxiv.org/abs/2403.07378)

执行与筛选规则：

1. 逐模块收集或读取输入，对所有初始候选计算 NMSE 和参数节省量。
2. 每个模块保留 dense，以及 1–2 个覆盖不同压缩强度的候选；优先选择误差增长较缓或曲线拐点附近的配置。
3. 若某候选节省参数更少、NMSE 又更高，可优先降低其后续评测优先级。
4. 从被淘汰的候选中分层抽取一小部分进入阶段 B，检查粗筛是否漏掉“局部误差大、最终影响小”的配置。

NMSE 不反映后续层对误差的放大或抵消，也不宜用一个未经验证的统一阈值决定所有模块的去留。这里的筛选是预算内的启发式决策；如果抽查出现明显漏选，应增加该类模块的候选覆盖。

输出：每个模块的“参数保留比例—局部 NMSE”曲线和待进行 KL 评估的候选集合。

## 5. 阶段 B：以原模型为 reference 测最终输出 KL

### 5.1 指标定义

设 $f_0$ 为未压缩模型，$f_{\ell,r}$ 为仅替换模块 $\ell$ 的模型。两者在相同文本前缀 $x_{<t}$ 上的下一 token 分布分别为 $p_{0,t}$ 和 $p_{\ell,r,t}$。定义：

$$
S_\ell(r)=
\frac{1}{N_{\mathrm{valid}}}
\sum_{x,t\;\mathrm{valid}}
\sum_{v\in\mathcal V}
p_{0,t}(v)
\log\frac{p_{0,t}(v)}{p_{\ell,r,t}(v)}.
$$

使用自然对数，结果单位为 nats/token。$S_\ell(r)$ 越小，表示该候选越能保留 reference 在校准文本上的预测分布。利用 reference logits 计算压缩模型 KL 已有公开实现可参考。[llama.cpp 的 KL 评估说明](https://github.com/ggml-org/llama.cpp/tree/master/tools/perplexity)

### 5.2 计算约定

- 使用 teacher forcing：向两个模型输入完全相同的 token 序列，批量计算各位置预测；不让两者各自自由生成后再比较。
- 默认温度 $T=1$。如额外研究其他温度，分别报告，不混合不同温度的排名。
- 使用 `log_softmax` 计算 log probability，KL 累加采用 FP32；先对词表求和，再除以有效 token 总数。
- 两模型使用相同 attention mask、position IDs 和评分位置。若同时计算 NLL，保证 logits 与下一 token 标签正确错位对齐。
- 对指令数据，可分别报告全序列 KL 和回答区域 KL；主筛选指标及其 mask 在实验前固定。
- 除均值外，记录序列级 KL 分布和主要数据类别的均值。若用 bootstrap 估计不确定性，以序列为重采样单位。

先测一次 dense 对 dense，检查 KL 是否接近数值零。若基线有明显偏移，先排查精度、随机性、缓存和 mask 差异。

### 5.3 怎样节省时间与显存

Reference 只需对同一输入计算一次，但完整 logits 缓存大小约为 $N_{\mathrm{tokens}}\times |\mathcal V|\times\text{每个数值的字节数}$。词表很大时，优先选择分批处理，或固定抽取部分评分位置，而不必长期保存全部 logits。

若抽取评分位置，仍保留这些位置所需的完整上下文；所有候选使用相同的位置样本。若存储低精度 logits，应先检查缓存引入的 KL 偏差。

缓存某个 block 的输入、mask 和位置状态，可让单模块候选从该 block 开始向后运行。第一版建议先用完整前向实现，确认结果后再做这一优化。计算后半部分时，受影响的下游激活和 KV 状态必须重新计算。

只保留 top-k 词后重新归一化，会改变 KL 的定义。如果采用 top-k 加剩余概率质量的合并近似，应明确标记为粗粒度 KL，并对部分候选核对完整词表结果。

### 5.4 候选排序

为每个候选同时记录 $S_\ell(r)$ 和节省量 $\Delta P_\ell(r)$。候选 A 若节省量不少于 B、KL 又不高于 B，则在这两个指标上优于 B；可优先把 A 送入任务确认。

不要把 NMSE、KL 和准确率任意加权成一个未经校准的总分。三者分别承担粗筛、输出一致性判断和任务确认。

## 6. 阶段 C：用任务曲线确认关键候选

这一阶段保留 CompactifAI 的“压缩强度—任务准确率”检验形式，但把评测预算集中在关键候选。

优先抽取以下模块与配置：KL 高、中、低各档代表；不同模块类型和网络深度的代表；NMSE 与 KL 排名分歧较大的候选；预计会进入最终模型且节省量较大的候选；粗筛抽查发现的例外。

对选中的模块，在固定任务开发集上测试 dense 与少数压缩强度，计算：

$$
\Delta A_\ell(r)=A(f_0)-A(f_{\ell,r}).
$$

准确率统一报告为百分数，下降量用“百分点”。任务可选 MMLU 或实际目标任务；使用固定开发子集时，清楚标注子集规模和抽样方式。若为补全曲线新增压缩点，应同步测量其 NMSE 与 KL。有限样本上的准确率和 KL 曲线不一定严格单调，不据此直接进行二分阈值搜索。

并排观察三条曲线：

1. 参数保留比例—NMSE；
2. 参数保留比例—最终输出 KL；
3. 参数保留比例—任务准确率。

用第三条曲线检查第二条的筛选价值。如果存在低 KL 但任务明显退化的候选，应检查数据覆盖、评分区域和能力类别，并增加相应类别的任务验证。校准集 KL 阈值应由开发集观察确定，不预设一个适用于所有模型的数值。

第一轮所有扫描点均不 healing，以控制成本并保证可比性；这是本方案的实验约定。如果最终部署需要 healing，再对少数完整方案按相同训练预算进行验证。

## 7. 阶段 D：分配压缩预算并验证组合效果

### 7.1 生成初始组合

每个模块最终可选集合中都保留 dense。给定全模型参数预算 $B$，可使用以下代理优化生成初始方案：

$$
\min_{r_1,\ldots,r_L}\sum_{\ell=1}^{L} S_\ell(r_\ell)
\quad\text{s.t.}\quad
P_{\mathrm{fixed}}+\sum_{\ell=1}^{L}P_\ell(r_\ell)\le B.
$$

$P_{\mathrm{fixed}}$ 包含未纳入压缩搜索的参数。任务开发集已发现明显退化的候选，应先排除或提高保留比例。

此处 KL 求和只是用于搜索的可分解代理目标，不能视作组合模型真实 KL。实现时可用离散预算搜索，或按“增量 KL / 增量节省参数”逐步选择；贪心选择不保证全局最优。若采用存储预算，应将 $P$ 替换为实际字节成本；延迟目标需要直接测量。

### 7.2 测量真实联合影响

组装初始方案后，相对同一个未压缩 reference 测量联合 KL，以及任务开发集表现。单模块误差可能在后续计算中放大或抵消，不能用单模块结果相加替代这一测试。

若不满足任务容差，可在当前组合模型中逐个恢复可疑模块或提高其键维度，重新测量 KL 改善和参数成本。这测量的是当前压缩状态下的条件影响，用于决定修复顺序。

建议优先检查阶段 C 发现的高风险模块、同一 block 内同时强压缩的模块，以及相邻的强压缩 block。修复后重新检查预算；若参数预算与任务容差无法同时满足，应如实记录当前候选集合下未找到可行方案。

### 7.3 Healing 与最终评测

保留少数满足约束的组合。若计划 healing，为所有对比方案使用相同训练数据与 token 预算，并分别记录 healing 前后的结果。

冻结配置和训练策略后，评测最终测试集上的任务表现、输出 KL、参数量、实际模型文件大小和目标硬件延迟。输出保真度、任务能力和运行效率分别报告。

## 8. 建议的最小实验配置

以下数值用于启动实验，后续根据排序稳定性和预算调整。

| 项目               | 起步设置                                                   |
| ------------------ | ---------------------------------------------------------- |
| 基本粒度           | 单个线性模块                                               |
| 张量化结构         | 每个模块固定结构，只改变键维度                             |
| 初始参数保留比例   | 约 0.8、0.6、0.4，加 dense                                 |
| 校准文本           | 32–64 段，常规场景每段 256–512 tokens                      |
| 阶段 A 保留数量    | 每模块 1–2 个压缩候选，加 dense                            |
| 粗筛抽查           | 抽查约 10% 被淘汰候选，并覆盖各模块类型                    |
| 阶段 B 主指标      | $T=1$ 的有效 token 平均前向 KL                             |
| 阶段 C 初始覆盖    | 8–16 个代表模块，每个 2–3 个压缩强度；若候选不足则全部测试 |
| 排名稳定性         | 用另一批校准文本复核，差异大的类别扩充样本                 |
| 最终完整方案       | 保留 2–3 个方案作联合验证                                  |
| Healing            | 仅用于少数完整方案，固定训练预算                           |
| 任务容差与资源预算 | 实验开始前写明，单位和评分协议保持一致                     |

候选总数很少时可直接从阶段 B 开始；阶段 A 只有在减少的候选前向成本超过激活收集和局部筛选成本时，才有实际收益。

## 9. 流程伪代码

```text
固定 reference、数据划分、张量化方式和资源预算
测量 reference 的开发集基线；验证 dense 对 dense 的 KL

for 每个模块 l:
    获取 reference 的模块输入
    构造候选，记录实际参数量与键维度
    计算每个候选的局部 NMSE
    保留不同强度的候选，并抽查部分淘汰项

for 每个进入阶段 B 的候选 (l, r):
    从原模型状态仅替换模块 l
    在相同校准输入上测最终输出 KL
    记录结果，随后还原模块 l

选取不同敏感度、类型、深度和指标分歧的候选
在固定任务开发集上测准确率与压缩强度曲线
根据任务验证结果修订候选集合

在资源预算下生成少数组合配置
for 每个组合配置:
    测真实联合 KL 与开发集任务表现
    若任务退化，测试局部恢复并调整组合
    重新检查资源预算
    如需 healing，使用统一预算训练并复测

冻结最终方案，在独立测试集上评测并报告
```

## 10. 记录内容与方法有效性验证

每条单模块记录至少包括：checkpoint 与精度、模块路径、张量化结构、完整键维度列表、实际参数保留比例、NMSE、KL 均值、任务准确率（未测则标记缺失）、数据与 mask 标识、分解耗时、评测耗时，以及是否经过 healing。

每条组合记录另加完整压缩配置、总参数与字节数、联合 KL、任务指标、healing 预算和目标硬件延迟。

为验证筛选是否值得，建议在一组可承担评测成本的代表模块上对比：

| 对比方法                            | 要回答的问题                                                 |
| ----------------------------------- | ------------------------------------------------------------ |
| 固定候选网格全部测任务准确率        | 作为本实验的较完整参照；不声称这是 CompactifAI 的官方逐点实现 |
| 仅 KL 筛选，再作任务确认            | KL 能减少多少任务评测，是否漏掉好配置？                      |
| NMSE 粗筛 + KL + 任务确认           | 加入局部筛选后是否进一步省时，漏选是否增加？                 |
| 各方法最终组合使用相同 healing 预算 | 筛选差异是否在恢复训练后仍有影响？                           |

比较时同时报告总分析耗时、完整前向处理的 token 数、任务评测候选数、峰值缓存占用，以及相同压缩预算下的最终任务表现。分析耗时应包含分解、数据处理、激活缓存与磁盘读写。

若第 $\ell$ 个模块有 $K_\ell$ 个压缩候选，逐点完整任务扫描的任务评测成本约为 $\sum_\ell K_\ell C_{\mathrm{task}}$。若阶段 A 连同抽查项共保留 $R_\ell$ 个候选，阶段 C 评测候选总数为 $Q$，则混合流程的成本可粗略记为：

$$
C_{\mathrm{hybrid}}
\approx C_{\mathrm{reference}}+C_{\mathrm{local}}
+\sum_\ell R_\ell C_{\mathrm{KL},\ell}
+Q C_{\mathrm{task}}+C_{\mathrm{joint}}.
$$

$C_{\mathrm{local}}$ 包括候选分解与局部筛选；缓存读写计入对应阶段。若使用 healing，应在两种流程中另行计入训练成本。

这只描述预算构成，不是加速保证。KL 与任务评测每次处理的数据量不同，减少的任务调用次数不能直接换算成总耗时加速比。实际收益需要在相同硬件、候选范围和最终质量约束下测量。